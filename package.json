{
  "name": "care-bed-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "test:launcher": "node --test scripts/dev.test.mjs",
    "test": "npm run test:launcher && npm --prefix app run test:run",
    "build": "npm --prefix app run build"
  }
}
