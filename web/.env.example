# 本地开发时复制为 web/.env 并填入。
# 这些是「服务端」环境变量：由 api/chat.ts 读取，永远不会打进前端 JS。
# Vercel 部署：在项目 Settings → Environment Variables 里配同样的名字即可。
GLM_API_KEY=
GLM_MODEL=glm-5.3-flash
GLM_API_URL=https://open.bigmodel.cn/api/paas/v4/chat/completions