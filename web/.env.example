# 复制此文件为 .env 并填入 API Key（本地开发）。
# Vercel 部署：在项目 Settings → Environment Variables 里配 VITE_GLM_API_KEY=你的key。
# 注意：VITE_ 前缀的变量会被打包进前端 JS，浏览器可见明文。
VITE_GLM_API_KEY=
VITE_GLM_MODEL=glm-5.3-flash
VITE_GLM_API_URL=https://open.bigmodel.cn/api/paas/v4/chat/completions
